--blips Ballas

Citizen.CreateThread(function()

    local ballasmap = AddBlipForCoord(97.04, -1933.24, 20.8)

    SetBlipSprite(ballasmap, 310)
    SetBlipColour(ballasmap, 27)
    SetBlipScale(ballasmap, 0.80)
    SetBlipAsShortRange(ballasmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Ballas")
    EndTextCommandSetBlipName(ballasmap)
end)

--fin blips Ballas

--blips Families

Citizen.CreateThread(function()

    local familliesmap = AddBlipForCoord(-144.70, -1601.57, 35.06)

    SetBlipSprite(familliesmap, 310)
    SetBlipColour(familliesmap, 25)
    SetBlipScale(familliesmap, 0.80)
    SetBlipAsShortRange(familliesmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Families")
    EndTextCommandSetBlipName(familliesmap)
end)

--fin blips Families

--blips Vagos

Citizen.CreateThread(function()

    local vagosmap = AddBlipForCoord(334.75, -2039.65, 21.1)

    SetBlipSprite(vagosmap, 310)
    SetBlipColour(vagosmap, 5)
    SetBlipScale(vagosmap, 0.80)
    SetBlipAsShortRange(vagosmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Vagos")
    EndTextCommandSetBlipName(vagosmap)
end)

--fin blips Vagos

--blips Marabunta

Citizen.CreateThread(function()

    local vagosmap = AddBlipForCoord(1254.06, -1576.02, 54.55)

    SetBlipSprite(vagosmap, 310)
    SetBlipColour(vagosmap, 77)
    SetBlipScale(vagosmap, 0.80)
    SetBlipAsShortRange(vagosmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Marabunta")
    EndTextCommandSetBlipName(vagosmap)
end)

--fin blips Marabunta

--blips Bloods

Citizen.CreateThread(function()

    local vagosmap = AddBlipForCoord(-1546.06, -399.18, 41.98)

    SetBlipSprite(vagosmap, 310)
    SetBlipColour(vagosmap, 1)
    SetBlipScale(vagosmap, 0.80)
    SetBlipAsShortRange(vagosmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Bloods")
    EndTextCommandSetBlipName(vagosmap)
end)

--fin blips Bloods

--blips Crips

Citizen.CreateThread(function()

    local vagosmap = AddBlipForCoord(378.84, -1799.55, 29.22)

    SetBlipSprite(vagosmap, 310)
    SetBlipColour(vagosmap, 77)
    SetBlipScale(vagosmap, 0.80)
    SetBlipAsShortRange(vagosmap, true)
    BeginTextCommandSetBlipName('STRING')
    AddTextComponentString("Crips")
    EndTextCommandSetBlipName(vagosmap)
end)

--fin blips Crips